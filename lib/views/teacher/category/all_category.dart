import 'package:flutter/material.dart';

class AllCategoryPage extends StatelessWidget {
  int? maxCount;
  AllCategoryPage({super.key, this.maxCount});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
